## Shared Flash Point ambiguity helper
- **Suggestion**: Extract the explicit Flash Point candidate ambiguity handling into a shared helper used by both the browser content script and the Node analysis model.
- **Rejection reason**: The two files run in different packaging/runtime contexts, and sharing code would broaden the extension surface for a small helper. The implementations now use the same rule and have tests covering both paths.
- **Rejected**: 1 time.
